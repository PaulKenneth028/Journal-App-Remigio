<h1> Categories </h1>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
        <% @tasks.each do |task| %>
            <tr>
                <td><%= task.name %> </td>
                <td><%= task.name %> </td>
                <td>
                <%= link_to "Edit", edit_task_path(task) %>
                <%= link_to "Show", task %>
                </td>
            </tr>
            <% end %>
    </tbody>
</table>

<%= link_to "New Task", new_task_path %>